# Hospital BI — Projeto-BI

Indicadores e **Visão Gerência** (React + Vite + API Express/TypeScript).

**Documentação para agentes (IA) e detalhe técnico:** [`agents.md`](./agents.md) — na **raiz**, **ao lado de** [`doc.md`](./doc.md) (fonte única: arquitetura, dados, arranque).

**Windows:** `iniciar-hospital-bi.bat` na raiz.

```bash
cd BACKEND && npm install && npm run dev
cd FRONTEND && npm install && npm run dev
```

## Licença / uso

Uso interno do projeto BI; ajustar conforme política da organização.
