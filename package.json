{
  "name": "projeto-bi-hospital",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "Orquestração na raiz (mesmo padrão que command-center-web: api + web).",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web",
    "api": "npm start --prefix api",
    "setup": "npm install --prefix web && npm install --prefix api"
  }
}
