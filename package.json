{
  "name": "novo-bi",
  "private": true,
  "version": "0.1.0",
  "description": "Novo BI v0 - workspace api/web em TypeScript",
  "scripts": {
    "postinstall": "npm install --prefix api && npm install --prefix web",
    "dev": "concurrently -k -n api,web -c blue,green \"npm run dev --prefix api\" \"npm run dev --prefix web\"",
    "build": "npm run build --prefix api && npm run build --prefix web",
    "start": "npm run start --prefix api"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=22"
  }
}
